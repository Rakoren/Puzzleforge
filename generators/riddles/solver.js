/**
 * Riddles — solve().
 * Answers are authored, so the solver surfaces them for the answer key.
 */
function solve(puzzle) {
  return { answers: puzzle.solution.answers };
}

module.exports = { solve };
