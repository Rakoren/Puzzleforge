/**
 * Math Puzzles — solve().
 * Answers are computed at generation time; the solver surfaces them.
 */
function solve(puzzle) {
  return { answers: puzzle.solution.answers };
}

module.exports = { solve };
